# 一期实施计划与验收

更新：2026-09-08。独立管理和受限模型适配开发版已实现，真实供应商及生产门槛仍待验收。最新运行/测试边界见 [开发版使用与验收](独立系统开发版使用与验收.md)。

## 已有基础

- [x] 平级项目、独立 API/数据库/知识 Worker。
- [x] Admin 登录、会话、CSRF；原知识页面/代理已退役，旧接口返回 410。
- [x] 知识 Key、范围权限、上传校验、幂等与来源修订唯一性。
- [x] 持久任务、模板草稿、版本发布、检索、下架/删除及可选 Embedding。
- [x] core 知识设置、钥匙串、手动预览上传、历史、重试与状态。
- [x] Compose 和可选模型网关模板；不代表模型业务完成。

## P0：知识管理独立

- [x] 知识独立管理 Web、本地管理员初始化、会话/CSRF/退出。
- [x] 迁入建库、知识 Key、收件箱、草稿、发布、检索调试与任务页面。
- [x] 来源拒绝审核及草稿/任务阻断，主要管理列表 offset/limit 分页；core 公共协议保持兼容。
- [ ] 资料所有者/空间权限、来源关系、软件版本/验证时间、失效/替代模型。
- [x] 新入口经独立 API/浏览器测试，Admin 知识页面/代理与共享网络依赖已退役；新增迁移保留旧数据，用户库尚未升级。

验收：停止 Admin 后知识仍可独立登录、建库、签发 Key、上传、整理、审核、发布、检索。不跨库访问，不默认复制密码/会话，既有资料与引用规则可追溯。

## P1：Admin 模型接入（受限协议开发版已实现）

- [x] 实现受限 Chat Completions 适配器；Admin 数据库为配置权威，双模拟渠道测试通过。
- [ ] 至少两家真实供应商 PoC；其他原生协议另行适配。
- [x] 兼容协议供应商 URL、Fernet Key、超时、模型列表探测、启停。
- [x] 对外模型别名、上游映射、切换渠道。
- [x] 管理员分配用户模型 Key：归属、权限、有效期、撤销、单进程 RPM。
- [x] 模型列表、JSON/SSE 转发、超时/取消处理；正常流和中断流已测，真实断连待验收。
- [x] 持久调用记录与当前页统计；usage 缺失为未知。全局聚合、图表和保留策略仍待做。
- [x] 默认不存正文/秘密，服务器主机白名单、禁重定向、客户端不能指定上游。

验收：至少两家目标供应商调用通过；统一地址/用户 Key 可供 core 使用，路由、权限、撤销、限流和监控有效。停知识服务不影响模型业务。模拟上游不能替代真实供应商验收；收费测试需开发凭据和预算。

## P2：一期联调与生产门槛

- [ ] core 桌面真实钥匙串/HTTP 上传与重试；契约烟测不能替代人工验收。
- [ ] Agent 检索引用，不把资料当执行授权或实时服务器事实。
- [ ] 独立索引构建代次、不可变配置绑定、重建切换；API/Worker 不一致时拒绝错标。
- [x] 先行防护：Worker 检查发布指纹，批次前后和落库前不一致即 INDEX_CONFIG_MISMATCH；保留旧发布版本，可恢复配置后重试。独立索引代次/重建仍未完成。
- [ ] PostgreSQL 版本化迁移、数据库侧检索、中文评测、容量/延迟指标。
- [ ] 开放多人前完成资源权限/空间隔离负向测试。
- [ ] 任务监控/退避、删除保留、备份恢复、TLS、轮换、许可/供应商条款核验。

继续回归旧测试，新增独立登录、两系统停机隔离、模型转发/SSE/监控、索引配置变更、权限及删除传播测试。临时测试不使用真实运维资料。

## 后续

AI 整理、多来源自动合并、文件解析、公众注册、支付/账本和长期记忆另行设计。基本用户模型 Key/转发/监控不能推迟，见 [后续待办](后续功能待办.md)。
